/**
 * SIMPLE VIDEO ANIMATION - Your Better Approach
 * Video stays visible, only hero moves over it, other sections come after
 */

gsap.registerPlugin(ScrollTrigger);
// GSAP animation temporarily disabled
